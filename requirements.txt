setuptools
# EfficientWord-Net currently requires numpy 1.22.0, so we pin to the
# compatible version to avoid resolver conflicts during installation.
numpy==1.22.0
elevenlabs
EfficientWord-Net==1.0.2
ai-edge-litert
librosa
pyaudio
pixel-ring
python-dotenv
